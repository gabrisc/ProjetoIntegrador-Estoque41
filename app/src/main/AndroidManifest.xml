<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.estoque411">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ListagemDeProdutosParaCompras"></activity>
        <activity android:name=".ListagemDeVendasRealizadas" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".EditarProdutoActivity" />
        <activity android:name=".ListagemDeProdutosActivity" />
        <activity android:name=".Resultados" />
        <activity android:name=".CadastrarVendaActivity" />
        <activity android:name=".CadastrarProdActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>